name: Build Android App

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install Bubblewrap
        run: npm install -g @bubblewrap/cli

      - name: Build APK
        run: |
          cd android
          export BUBBLEWRAP_KEYSTORE_PASSWORD="glacier2026"
          export BUBBLEWRAP_KEY_PASSWORD="glacier2026"
          # Try to build (Bubblewrap build will generate the project structure if missing)
          bubblewrap build --yes

      - name: Upload APK
        uses: actions/upload-artifact@v3
        with:
          name: glaciergoals-apk
          path: android/*.apk

      - name: Create Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/heads/main')
        with:
          tag_name: v1.0.${{ github.run_number }}
          name: GlacierGoals v1.0.${{ github.run_number }}
          files: android/*.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
